# vscode
.vscode

# Intellij
*.iml
.idea

# npm (we don't use it but keep for future)
node_modules

# Don't include the compiled main.js in the repo.
# It should be uploaded to GitHub releases instead.
# Note: our main.js IS the source (pure-JS, no build), so we DO commit it.
# Uncomment below if you switch to a TypeScript build:
# main.js

# Exclude sourcemaps
*.map

# obsidian
data.json

# macOS Finder
.DS_Store

# Custom
*.tmp